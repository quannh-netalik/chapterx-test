import { Modal } from 'antd';
import styled from 'styled-components';

export const CustomModal = styled(Modal)`
  .ant-modal-title {
    padding: 24px 0;
    font-size: 16px;
    font-weight: bold;
  }
`;
